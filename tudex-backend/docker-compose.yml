version: '3.8'

services:
  web:
    image: httpd:2.4-alpine
    container_name: tudex-apache-webserver
    ports:
      - "8081:80"
    volumes:
      - ./httpd.conf:/usr/local/apache2/conf/httpd.conf:ro
      - ./:/usr/local/apache2/htdocs
    restart: unless-stopped
